connection.host = ""
connection.username = ""
connection.password = ""
connection.port = 22
connection.database.localPort = 3307
connection.database.remotePort = 3306

database.url = ""
database.username = ""
database.password = ""

database.maxPoolSize.admin = 0
database.maxPoolSize.user = 0
database.maxPoolSize.userNotification = 0

database.maxActive = 15000

database.driver {
    ~default = "com.mysql.jdbc.Driver"

    ~test = "org.h2.Driver"
}

database.dialect {
    ~default = "org.hibernate.dialect.MySQLMyISAMDialect"

    ~test = "org.hibernate.dialect.MySQLDialect"
}

database.startupAction {
    ~default = "update"

    ~test = "create-drop"
}

database.showSql = "false"
database.formatSql = "false"
database.id.newGeneratorMappings = "false"

client.admin.interface.languages = [""]
client.admin.interface.defaultLanguage = ""

gms.key = ""

platform.languages = [""]
platform.defaultLanguage = ""

images.articlesIcons.location {
    ~default = "/root/Omega/Articles"

    ~dev = "C:/Omega/Articles"
}

images.answersIcons.location {
    ~default = "/root/Omega/Answers"

    ~dev = "C:/Omega/Answers"
}

images.categoriesIcons.location {
    ~default = "/root/Omega/Categories"

    ~dev = "C:/Omega/Categories"
}

images.reports.location {
    ~default = "/root/Omega/Reports"

    ~dev = "C:/Omega/Reports"
}

images.brandingIcon.location {
    ~default = "/root/Omega/Branding/Icon"

    ~dev = "C:/Omega/Branding/Icon"
}

images.brandingBackground.location {
    ~default = "/root/Omega/Branding/Background"

    ~dev = "C:/Omega/Branding/Background"
}